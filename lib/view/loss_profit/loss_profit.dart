import 'package:flutter/material.dart';

class LossProfit extends StatelessWidget {
  const LossProfit({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(child: Scaffold(
      appBar: AppBar(
        title: Text("Loss profit"),
      ),
      body: Column(
        children: [

        ],
      ),
    ));
  }
}
